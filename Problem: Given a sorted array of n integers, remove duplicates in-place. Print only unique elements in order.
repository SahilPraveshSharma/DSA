#include <stdio.h>

int main() {
    int n;
    
    
    scanf("%d", &n);
    
    int arr[n];
    
    
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    if (n == 0) return 0;  // If empty input, do nothing
    
    int uniqueIndex = 0;  // Position to store unique elements
    
  
    for (int i = 1; i < n; i++) {
        if (arr[i] != arr[uniqueIndex]) {
            uniqueIndex++;
            arr[uniqueIndex] = arr[i];
        }
    }
    
  
    for (int i = 0; i <= uniqueIndex; i++) {
        printf("%d ", arr[i]);
    }
    
    return 0;
}
