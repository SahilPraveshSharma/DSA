#include <stdio.h>

int power(int a, int b) {
    if (b == 0)  // Base case: a^0 = 1
        return 1;
    return a * power(a, b - 1);
}

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    
    int result = power(a, b);
    printf("%d\n", result);
    
    return 0;
}
